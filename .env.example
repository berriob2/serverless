# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_NAME=serverless-converter-${STAGE}-uploads

# Application Settings
TMP_DIR=/tmp

# Security note: Never commit actual credentials to your repository
# For local development, use AWS profiles or environment variables
# For production, use AWS Lambda environment variables or AWS Secrets Manager